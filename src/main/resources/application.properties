# Optional configuration overrides for local development.
# Environment variables and .env values take precedence.

# TELEGRAM_BOT_TOKEN=
# TELEGRAM_BOT_USERNAME=

DEFACE_ENDPOINT=http://localhost:8000/api/deface-image

# Comma-separated allowed values (override as needed).
ALLOWED_FILTER_NAMES=blur,pixelate,line_mosaic,facet_effect,verwischung_1
ALLOWED_PASTE_STYLES=feathered,hard

# Defaults used when not specified via env or .env.
DEFAULT_FILTER_NAME=blur
DEFAULT_PASTE_STYLE=feathered
